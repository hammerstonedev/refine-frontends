const numericSingleProps = {
  props: {
    value: {
      type: String,
      required: false,
    },
  },
};

const numericDoubleProps = {
  props: {
    from: {
      type: String,
      required: false,
    },
    to: {
      type: String,
      required: false,
    },
  },
};

export { numericSingleProps, numericDoubleProps };

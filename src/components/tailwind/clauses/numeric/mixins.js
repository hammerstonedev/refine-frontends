const numericSingleProps = {
  props: {
    value: {
      type: Number,
      required: false,
    },
  },
};

const numericDoubleProps = {
  props: {
    from: {
      type: Number,
      required: false,
    },
    to: {
      type: Number,
      required: false,
    },
  },
};

export { numericSingleProps, numericDoubleProps };

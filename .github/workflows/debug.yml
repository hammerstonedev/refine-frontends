name: Test
on: [ push, pull_request ]
#on:
#  push:
#    tags:
#      - 'vue/*'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v3
        with:
          node-version: 14
      - run: npm install --also=dev
      - run: npm run debug-tags